# Release scope fixture

This valid-only fixture exists so the repository validator harness tracks
`validate-release-scope.py`. The validator evaluates the repository scope,
not this file's contents.
