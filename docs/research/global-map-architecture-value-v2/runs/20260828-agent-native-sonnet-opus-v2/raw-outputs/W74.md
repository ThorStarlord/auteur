**Recommendation: publish-verified-testimony**

**Why:** The active commitment is `contested-history`, carried now by two accepted, currently-live facts: `monastery-testimony` (reactivated) and `archive-protected` (current, treaty-protected). The planning intent is explicit — return the testimony to public memory *without breaking* the protected archive. `archive-protected` exists precisely because the earlier `admission-retracted` history made the evidence vulnerable, so the treaty protection is the load-bearing constraint here, not incidental. `publish-verified-testimony` keeps the original evidence secured under that protection while authenticating and releasing the testimony itself — it satisfies the intent without touching the thing the treaty exists to guard. `stage-protected-hearing` also stays inside the treaty's protections technically, but its tradeoff — revealing which records carry the strongest evidence — cuts directly against the evidentiary chain the protection was built to preserve, and against the interpretive throughline that future payoff depends on preserving that chain to authenticate the testimony.

**Principal tradeoff:** publish-verified-testimony delays public release until verification is complete — slower, but it does not expose or differentiate the archive's records, so the evidentiary chain stays intact.

**Deliberately excluded:**
- `burn-archive` — never accepted, and independently incompatible with `archive-protected` per the explicit derived relationship; not a live option.
- `ally-militia` — never accepted; excluded on the same basis.
- `founding-record`, `admission-retracted`, `public-admission` — accepted but dormant/superseded history; they explain why the archive is protected today but are not independent active constraints on this choice.
- `broken-lantern` / `repaired-lantern` — neither is active; `repaired-lantern`'s recency is explicitly noted as not conferring relevance.
- The "official history vs. lived memory" thematic framing — interpretive researcher context, used only to weigh the tradeoff, not treated as canon or a hard constraint.

This is a non-authoritative planning recommendation; it creates no Book Direction and modifies no Canonical State.
