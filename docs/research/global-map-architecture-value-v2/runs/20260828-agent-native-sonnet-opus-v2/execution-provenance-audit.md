# Architecture Value Experiment V2 — Agent-Native Replication — Execution Provenance Audit

**Run ID:** `20260828-agent-native-sonnet-opus-v2`
**Execution base (the `main` commit this replication branched from):** `1053154f3d23893e2ce6a4e48fa5cb16b2d459ed`
**Frozen narrative/source revision (all three conditions derive from this fixture snapshot):** `3cc497583dcb9b3bcee5ba273ee8bcbf27cbba41` (PR #143 merge)
**Orchestration branch:** `claude/auteur-architecture-v2-replication-uwlko2`
**Auditor:** the orchestrator itself, mechanically, immediately after each phase (self-audit; no independent human review had occurred as of original authorship). **This document was revised by a subsequent evidence-reconciliation pass — see `evidence-reconciliation.md` for the itemized corrections and the reasoning behind each; several claims below were narrowed accordingly.**

This document supersedes the *method* used by `20260827-muse-spark-v2` (SYNTHETIC / SIMULATED EXECUTION, provenance class C — see that run's own `execution-provenance-audit.md`) with a genuine model-invocation mechanism. It does not use, reproduce, or contradict that run's illustrative pattern.

## 1. Phase 0 — inference backend qualification

Candidate mechanisms inspected: the Claude Code Agent tool (`Agent`, spawns an isolated sub-agent with `model` and `subagent_type` parameters), and no other explicit inference mechanism was available in this session (no direct provider API key/tooling was present; the Agent tool was the only qualified isolated-inference mechanism found).

Properties checked against the frozen requirements (see task instructions §5):

**This table was revised during the `evidence-reconciliation.md` pass (see that document for full detail); several entries below are narrower than originally stated.**

| requirement | finding |
|---|---|
| fresh inference context per invocation | CONFIRMED for the tested property — canary test below showed zero cross-contamination between two parallel invocations with distinct secret nonces, and none of the 90 experimental invocations used `to:`/resume |
| no hidden inheritance of parent's experimental reasoning or another worker's experimental conversation | CONFIRMED as far as testable — canary agents had no knowledge of each other's nonce or of any prior conversation |
| exact control over the experimental *task input* supplied to the worker | CONFIRMED for the packet content specifically: each worker was instructed to make exactly one `Read` call against a single frozen packet file written by the orchestrator before any worker ran; packet content is therefore byte-exact and hash-verifiable, not paraphrased into the prompt. **NOT independently confirmed:** whether the packet file was the worker's *entire* context, or whether ordinary `general-purpose` sub-agent startup context (system prompt, tool definitions, repository `CLAUDE.md`/agent instructions, workspace metadata) was additionally present. If present, it was uniform across all 90 invocations. See `evidence-reconciliation.md` §B. |
| no exposure to A/B/C identity for generator workers | **NOT FULLY CONFIRMED.** Generator packet *bodies* contain no condition label, must-not-miss text, or forbidden-assumption text (verified by leakage audit). However, the generator packet *filenames* are condition-correlated (`P01-A.txt`, `P04-C.txt`, …) and each generator's delegation prompt named that exact file path — so the condition letter was present as a plain-text token in the string given to every generator sub-agent. Frozen V2 itself does not require generator-side A/B/C blinding (only evaluator-side); this is an agent-native execution-contract deviation / leakage risk, not a frozen-V2 invalidation. See `evidence-reconciliation.md` §C. |
| no exposure to the evaluator rubric for generator workers | CONFIRMED — generator packets contain only `decision-probes.md`-equivalent content (horizon facts/intent/question/options/generic contract); rubric and hidden must-not-miss/forbidden text never appear in any of the 15 generator packet files (bodies or filenames) |
| exact capture of the returned response | CONFIRMED for 44 of 45 evaluations and all 45 generations, verbatim. One evaluation (`E61`) was returned truncated by the sub-agent; the orchestrator's normalized `blind-evaluation/E61.json` both closed the JSON syntax and paraphrased the free-text rationale field (structured judgment fields unchanged). The original truncated raw text is separately preserved verbatim in `blind-evaluation/E61.raw.txt`. See `evidence-reconciliation.md` §G. |
| distinct invocation, distinguishable from parent prose | CONFIRMED — each `Agent` result carries a distinct `agentId` and `subagent_tokens`/`tool_uses`/`duration_ms` usage block, structurally separate from orchestrator output |
| model identity as far as exposed | The orchestrator supplied the request-side parameter `model: "sonnet"` (generator) and `model: "opus"` (evaluator) on every call; the tool does not additionally echo back a resolved model-version string in its result. **Corrected claim:** generator requested runtime model alias = `sonnet`; evaluator requested runtime model alias = `opus`; exact resolved provider model-version = UNAVAILABLE / NOT INDEPENDENTLY OBSERVABLE FROM THIS EXECUTION RECORD. Any mention elsewhere of "claude-sonnet-5"/"claude-opus-5" is documentation-derived context (what these aliases are currently known to resolve to), not execution-observed provenance. See `evidence-reconciliation.md` §D. |
| stable model selection across required calls | CONFIRMED at the request-alias level — `model` parameter was identical (`sonnet`) across all 45 generation calls and identical (`opus`) across all 45 evaluation calls. Not confirmed at the resolved-provider-version level (see above). |
| fresh-context / no-carry-over across invocations | CONFIRMED by canary and by the absence of any `to:`/resume parameter in any of the 90 calls |
| tool restriction | PARTIAL / DOCUMENTED LIMITATION — the `Agent` tool schema exposes no explicit tool-allowlist parameter; `general-purpose` sub-agents have full tool access by definition. Isolation was instead enforced by instruction ("use Read exactly once on this path, no other tool") and verified post hoc: every one of the 90 invocation results reported `tool_uses: 1`, meaning no worker used any tool beyond the single instructed `Read` call. This is an instruction-enforced, not sandbox-enforced, restriction — logged as a limitation, not silently assumed |
| invocation metadata sufficient for audit | CONFIRMED — `agentId`, `subagent_tokens`, `tool_uses`, `duration_ms` are present per invocation; raw invocation logs are the tool-call transcript of this orchestration session (not separately exported, since this session's own transcript is the provenance record). This session-transcript basis is also the limit of the blinding-chronology proof — see `evidence-reconciliation.md` §F for why this is session-supported/self-audited rather than independently Git-anchored. |

## 2. Canary (non-experimental) evidence

Two canary sub-agents were spawned in parallel before any experimental packet existed, each given a distinct secret nonce (`CANARY-ALPHA-7f3d9`, `CANARY-BETA-2c8e1`) and told not to use any tool. Results:

- Each agent returned only its own nonce; neither mentioned or leaked the other's nonce — confirms fresh, isolated context.
- `tool_uses: 0` for both — confirms instruction-based tool restriction is followed when no file access is required.
- The two agents were run on different `model` values (`sonnet`, `opus`) and produced stylistically distinct answers, consistent with genuine distinct model invocations rather than templated/deterministic output.
- Distinct `agentId`s (`a639edb4bfdd01ac3`, `a7b59b2e8e6a41cef`) confirm invocation-level distinguishability.

Qualification verdict: **Agent-tool sub-agent spawning is qualified as the isolated inference mechanism for this replication**, with the tool-restriction limitation above disclosed rather than assumed away.

## 3. Selected backends (frozen for this run)

- **Generator:** Agent tool, `subagent_type: general-purpose`, requested runtime model alias `sonnet` (documentation context: currently resolves to claude-sonnet-5 — not an execution-observed fact, see item 6 below). Alias frozen (identical) for all 45 generation invocations.
- **Evaluator:** Agent tool, `subagent_type: general-purpose`, requested runtime model alias `opus` (documentation context: currently resolves to claude-opus-5). Alias frozen for all 45 evaluation invocations, distinct from the generator's alias per the frozen preference.

## 4. Known limitations of this mechanism (disclosed, not hidden)

1. **Sampling parameters not controllable.** The Agent tool exposes no `temperature`/`top_p`/`seed`/`max_output_tokens` parameter. The frozen V2 control variables (`temperature 0.2, top_p 1.0, max_output_tokens 1200, tools none`) could not be applied or verified. This is recorded as a material deviation in `post-unblind/invalidation-audit.json`; it is uniform across A/B/C so it is not a between-condition confound, but it does mean the 3 repetitions per cell are not temperature-pinned repeats.
2. **Tool restriction is instruction-enforced, not sandboxed.** No hard mechanism prevents a worker from calling other tools; compliance was verified after the fact via `tool_uses` counts (all 90 invocations showed exactly 1), not guaranteed in advance. A future replication with a stricter no-tools sub-agent type, if one becomes available, would remove this limitation.
3. **No provider-side response ID or token-usage confirmation, and no response-side model-version confirmation.** `subagent_tokens` reflects the sub-agent's own context usage, not a provider request/response ID in the sense of a raw HTTP API call, and the tool never echoes back a resolved model-version string. Both are disclosed as `RUNTIME-REPORTED` (request-side only), not `PROVIDER-REPORTED`.
4. **Single orchestration session, session-supported chronology only.** All 90 invocations were issued from one orchestrator session/transcript; that transcript, plus the deterministic step-ordering of `scripts/freeze_and_unblind.py`, is the provenance record for invocation timing, exact prompts, and freeze-before-unblind ordering. There is no independently Git-anchored pre-unblind commit — all experimental artifacts (blind evaluations, sealed map, and post-unblind joins alike) landed in a single commit. This is disclosed as session-supported/self-audited chronology, not independently auditable chronology.
5. **Sub-agent startup context not independently verified.** Whether a fresh `general-purpose` sub-agent additionally received ordinary runtime context (system prompt, `CLAUDE.md`, tool definitions, repository metadata) beyond the single instructed packet file was not inspected. If present, it was uniform across all 90 invocations.
6. **Generator packet filenames were condition-correlated.** The 15 generator packet files are named `{probe}-{condition}.txt` (e.g. `P01-A.txt`), and each generator's delegation prompt named that exact path — so the condition letter A/B/C was present as a plain-text filename token in every generator invocation, even though it never appeared in the packet body and the worker was never asked to interpret it. Frozen V2 does not require generator-side A/B/C blinding (only evaluator-side, which was maintained: evaluator-facing blind-packet filenames use only opaque IDs). This is an agent-native execution-contract deviation / leakage risk, disclosed rather than assumed away — see `evidence-reconciliation.md` §C for the full reasoning.

None of these limitations were treated as blockers per the frozen instruction ("if it fails because context isolation, model stability, output capture, or auditability cannot be established: do not force it") — the properties that matter most (fresh context and no cross-worker/cross-orchestrator inheritance, no rubric/must-not-miss/forbidden exposure to generators, exact and hashable control of the packet-body task input, verbatim capture of 44/45 outputs plus a disclosed and separately-preserved exception for the 45th, and a fixed request-side model alias per role) were all confirmed. Items 1–6 above are real and are disclosed precisely so a human reader can weigh them, rather than being folded into a single vague "one deviation" statement or silently assumed away.
