# Character and Relationship Trajectories V1

Character and relationship state is explicit Realization data. A state
projection never infers canonical facts from prose; unknown values remain
unknown. Relationship transitions should identify a causal reference when the
state changes. Missing references produce derived findings, not automatic
repairs.
