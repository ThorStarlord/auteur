# Epistemic State V1

Knowledge facts identify an observer, proposition, event index, source event,
and value (`known`, `unknown`, or `believed_false`). Reader, author, character,
or faction knowledge can use the same contract. The projection is read-only and
supports dramatic-irony and reveal diagnostics without claiming that omitted
facts are false.
