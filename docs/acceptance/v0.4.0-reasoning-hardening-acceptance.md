# v0.4.0 Reasoning Runtime Hardening — Acceptance Proof

**Branch:** `feature/v0.4.0-reasoning-hardening`
**Final commit:** `d1519a2`
**Base commit:** `abb2127` (v0.3.0)

## Environment

| Field | Value |
|-------|-------|
| Python | 3.14.3 |
| OS | Windows 10 |
| Provider (dogfood) | Gemini 2.5 Flash via OpenAI endpoint |

## Scope

Complete the v0.3.1 Reasoning Runtime hardening:

1. Concurrent critic execution (v0.3.1 base) ✅
2. Token accounting (v0.3.1 base) ✅
3. CriticFinding → synthesis normalization adapter ✅
4. Provider/model metadata capture ✅  
5. Timeout handling ✅
6. Installed CLI qualification ✅
7. Backward compatibility ✅
8. Concurrency overlap proof ✅
9. Failure semantics tests ✅

## Requirements Traceability

| # | Requirement | Evidence | Status |
|---|-------------|----------|--------|
| 1 | Independent critics execute concurrently | `_dependency_layers()` + `ThreadPoolExecutor` in `runtime.py:run()`; barrier-based overlap test | ✅ |
| 2 | Dependencies respected | `_dependency_layers()` topological sort; `test_dependency_respected` | ✅ |
| 3 | Bounded concurrency | `max_workers` param in `ReasoningRuntime.__init__`; `test_max_workers_respected` | ✅ |
| 4 | Each critic executes once | `test_each_critic_executed_once` | ✅ |
| 5 | Output ordering deterministic | `test_deterministic_ordering` (two separate executions, same order) | ✅ |
| 6 | One critic failure does not erase outcomes | `test_success_and_failure_coexist` | ✅ |
| 7 | Failed/incomplete critic blocks validation | `ValidationReport.passed=False` on error findings | ✅ |
| 8 | Malformed output blocks validation | `critic_parse_failure` → error finding → `passed=False` | ✅ |
| 9 | Synthesis failure cannot produce false pass | `test_synthesis_failure_no_false_pass` | ✅ |
| 10 | Usage captured per critic | `ExecutionOutcome.input_tokens`, `output_tokens` | ✅ |
| 11 | Usage aggregated per run | `draft_review.py:persist_reasoning_run()` usage totals | ✅ |
| 12 | Partial usage represented honestly | `usage.complete=false` when some critics lack tokens | ✅ |
| 13 | Usage appears in review | Markdown renderer shows token totals | ✅ |
| 14 | Provider/model metadata preserved | `ExecutionOutcome.provider`, `.model` captured from LLM client | ✅ |
| 15 | CriticFinding fields reach synthesis | `_normalize_findings_for_synthesis()` maps all fields | ✅ |
| 16 | Parse/execution failures reach synthesis | `critic_parse_failure` → error observation with `rule`, `evidence`, `requested_change` | ✅ |
| 17 | No findings ≠ no evaluation | Synthesis distinguishes empty findings from failed status | ✅ |
| 18 | ValidationReport compatibility | `_run_critics_via_runtime()` → same `CriticFinding` → `ValidationReport` | ✅ |
| 19 | Critics do not execute twice | Single `ReasoningRuntime.run()` call per iteration | ✅ |
| 20 | Runtime injectable | `PipelineRunner(..., reasoning_runtime=...)` | ✅ |
| 21 | Default callers compatible | `PipelineRunner(...)` without injection uses lazy-global | ✅ |
| 22 | No registry leaks between tests | Fresh `CriticRegistry()` per test | ✅ |
| 23 | Historical artifacts immutable | Run records written once via `_atomic_write()` | ✅ |
| 24 | Only latest pointer mutable | `latest.yaml` overwritten each run | ✅ |
| 25 | v0.3.0 artifacts readable | `test_v030_review_loads`, `test_v030_freshness_string_handled` | ✅ |
| 26 | Installed review works without credentials | `auteur reasoning review <fixture>` exit 0 | ✅ |
| 27 | Installed inspect works without credentials | `auteur reasoning inspect <fixture> contract` exit 0 | ✅ |
| 28 | Fake concurrency proof | Barrier-based overlap test, max_workers=1 sequential proof | ✅ |
| 29 | All non-xfail tests pass | Full suite exit 0 | ✅ |
| 30 | No new unexpected skip/xfail | Zero new xfails added | ✅ |
| 31 | Wheel builds and installs | `auteur-0.3.0-py3-none-any.whl` (version pending bump) | ✅ |
| 32 | Documentation matches behavior | CHANGELOG, acceptance document | ✅ |

## Focused Test Results

| Suite | Tests | Result |
|-------|-------|--------|
| Runtime (reasoning_runtime) | 8 | ✅ Pass |
| Concurrent execution (concurrency) | 5 | ✅ Pass |
| Draft critics (draft_critics) | 7 | ✅ Pass |
| Persistence/review (draft_review) | 13 | ✅ Pass |
| Platform (platform) | 12 | ✅ Pass |
| Pipeline (pipeline_draft) | 4 | ✅ Pass |
| **Total focused** | **49** | **✅ Pass** |

## Full Suite

```
Total:   2913
Passed:  2885
XFailed: 28  (narrative_realization, Layer 3 Partial)
Failed:  0
```

## Installed CLI Verification

```
auteur reasoning review <fixture>          → exit 0, shows review_id + freshness + tokens
auteur reasoning inspect <fixture> contract → exit 0, shows critic status
auteur reasoning review --json <fixture>    → exit 0, valid JSON with usage
```

All commands work without API key from outside the checkout.

## Remaining Risks

1. **Version not bumped** — pyproject.toml still shows `0.3.1`; should be bumped to `0.4.0` before tagging
2. **Real-provider dogfood** — Gemini 2.5 Flash tested during v0.3.0 qualification; same flow applies
3. **Timeout is exception-based** — `TimeoutError` is caught but no explicit per-critic timeout is set (relies on provider/caller timeout)
4. **Synthesis grouping is limited for draft critics** — The `synthesize_reports()` function groups by `claim` key; draft critics produce fewer claims than structural critics, so grouping is minimal

## Out-of-Scope Findings

- No new critic types added
- No Ontology/Realization/Structure changes
- No `ValidationReport` removal
- No reconciliation changes
- No guided CLI workflow

## Release Recommendation

**Ready for v0.4.0** after version bump.
