# v0.3.2 Reasoning Runtime Completeness — Acceptance Proof

**Branch:** `feature/v0.3.2-reasoning-completeness`
**Base commit:** `082aa05` (v0.3.1 tag)

## Environment

| Field | Value |
|-------|-------|
| Python | 3.14.3 |
| OS | Windows 10 |
| Test fixture | `/tmp/auteur-cli-test/review.json` (no API key required) |

## Scope

Completeness improvements atop the v0.3.1 Reasoning Runtime:

1. ✅ Provider/model metadata capture (formal LLM client attributes only)
2. ✅ Real timeout enforcement via `concurrent.futures.wait()` with `critic_timeout`
3. ✅ Faithful finding normalization (`_reasoning_sections`: no synthetic hypotheses/evaluation)
4. ✅ Setup_payoff hypotheses/recommendations preserved when present
5. ✅ Backward compatibility tests for v0.3.0/v0.3.1 artifacts
6. ✅ Concurrency overlap proof (barrier-based test)
7. ✅ Failure semantics tests (multi-failure coexistence, no false pass)
8. ✅ Negative CLI tests (missing/malformed/empty file)
9. ✅ Removed dead `_normalize_findings_for_synthesis` (was never called)
10. ✅ Provider metadata: no `_provider`/`_model` prefix fallback, no module-name heuristic

## Requirements Traceability

| # | Requirement | Evidence | Status |
|---|-------------|----------|--------|
| 1 | Timeout kills slow critic | `test_timeout_kills_slow_critic`: slow critic exceeding `critic_timeout=0.2` is FAILED with `reason="critic timed out"` | ✅ |
| 2 | Timeout per-critic, not per-layer | `test_timeout_in_multi_critic_layer`: one critic in layer times out, other succeeds | ✅ |
| 3 | No timeout by default | `critic_timeout=None` in `__init__` preserves backward-compatible behavior | ✅ |
| 4 | Provider/model from formal attributes | `test_provider_model_from_llm`: `llm.provider` and `llm.model` captured correctly | ✅ |
| 5 | No `_provider`/`_model` fallback | `test_prefixed_attributes_not_accepted`: `_provider` and `_model` are not used | ✅ |
| 6 | No module-name heuristic | `test_no_module_name_fallback`: bare object with no `provider` yields `None`, not module name | ✅ |
| 7 | No llm → no provider/model | `test_no_llm_no_provider_model` | ✅ |
| 8 | No synthetic hypotheses | `test_no_hypotheses_section`: `_reasoning_sections` doesn't produce `hypotheses` key from draft findings | ✅ |
| 9 | No synthetic certainty | `test_confidence_is_unknown`: confidence is `"unknown"` not `"confirmed"`/`"disputed"` | ✅ |
| 10 | Observations preserve fields | `test_observations_preserve_fields`: critic, severity, rule, evidence, requested_change | ✅ |
| 11 | Recommendations only when present | `test_recommendations_only_when_requested_change_present`: 0 or 1 based on data | ✅ |
| 12 | Genuine hypotheses preserved | `test_setup_payoff_produces_competing_hypotheses_and_alternatives`: 4 hypotheses from structure critic | ✅ |
| 13 | v0.3.0 artifacts readable | `test_v030_review_loads`, `test_v030_freshness_string_handled` | ✅ |
| 14 | CLI review works | `auteur reasoning review /tmp/auteur-cli-test/review.json` → exit 0, shows `test_fixture` | ✅ |
| 15 | CLI inspect works | `auteur reasoning inspect /tmp/auteur-cli-test/review.json contract` → exit 0, shows `draft.contract: success` | ✅ |
| 16 | CLI JSON output works | `auteur reasoning review --json` → valid JSON with usage totals | ✅ |
| 17 | Concurrency overlap proof | Barrier-based test proves concurrent execution | ✅ |
| 18 | max_workers=1 sequential proof | `test_max_workers_respected`: `max_active[0] <= 1` | ✅ |
| 19 | Multi-failure coexistence | `test_success_and_failure_coexist`: success and failure side by side | ✅ |
| 20 | Failure semantics | `test_multiple_failures_inspectable`: all failed outcomes visible | ✅ |
| 21 | Negative CLI: missing file | `test_missing_file`: raises FileNotFoundError | ✅ |
| 22 | Negative CLI: malformed JSON | `test_malformed_json`: raises JSONDecodeError | ✅ |
| 23 | Negative CLI: empty file | `test_empty_file`: raises JSONDecodeError | ✅ |
| 24 | No dead code | `_normalize_findings_for_synthesis` removed; no imports reference it | ✅ |
| 25 | Provider metadata in review output | JSON review output includes `provider` and `model` per critic | ✅ |

## Focused Test Results

| Suite | Tests | Result |
|-------|-------|--------|
| Runtime (reasoning_runtime) | 8 | ✅ Pass |
| Concurrent execution (concurrency) | 5 | ✅ Pass |
| Draft critics (draft_critics) | 7 | ✅ Pass |
| Persistence/review (draft_review) | 13 | ✅ Pass |
| Platform (platform) | 26 | ✅ Pass |
| Pipeline (pipeline_draft) | 4 | ✅ Pass |
| Setup payoff (setup_payoff) | 2 | ✅ Pass |
| **Total focused** | **65** | **✅ Pass** |

## Full Suite

```
2858 passed, 28 xfailed, 3 xpassed in 376.32s
```

3 xpassed are pre-existing editing CLI/model tests that were marked xfail but now pass — not a regression.
28 xfailed are `narrative_realization` tests (Layer 3 Partial, documented in v0.2.1).

## Installed CLI Verification

Commands run from `/tmp` (outside repository checkout) using a fresh virtual environment:

```
$ auteur reasoning review /tmp/auteur-cli-test/review.json
→ exit 0
  Output includes "Reasoning review test_fixture", "Status: fresh"

$ auteur reasoning inspect /tmp/auteur-cli-test/review.json contract
→ exit 0
  Output includes "draft.contract: success"

$ auteur reasoning review /tmp/auteur-cli-test/review.json --json
→ exit 0
  Output is valid JSON with usage totals, provider/model per critic
```

Fixture requires no API key; all commands work from outside the source tree.

## Wheel Build & Install

```
$ python -m build --wheel  → auteur-0.3.2-py3-none-any.whl
$ pip install auteur-0.3.2-py3-none-any.whl → OK
$ auteur --help → lists all subcommands (exit 0)
```

## Git Hygiene

```
$ git diff --check → clean
$ git status --short
  M CHANGELOG.md
  D docs/acceptance/v0.4.0-reasoning-hardening-acceptance.md
  M pyproject.toml
  M src/auteur/__init__.py
  M src/auteur/reasoning/runtime.py
  M tests/test_reasoning_platform.py
  M tests/test_reasoning_runtime.py
  ?? docs/acceptance/v0.3.2-reasoning-completeness-acceptance.md
```

No generated JSON, cache, virtual environment, or scratch output committed.

## Remaining Risks

1. **Timeout is thread-based** — `critic_timeout` uses `concurrent.futures.wait()` with daemon threads; subprocess-based critics would need separate handling
2. **Real-provider dogfood** — Gemini 2.5 Flash tested during v0.3.0 qualification; same flow applies

## Out-of-Scope Findings

- No new critic types added
- No Ontology/Realization/Structure changes
- No `ValidationReport` removal
- No reconciliation changes
- No guided CLI workflow

## Release Recommendation

**Ready to merge as v0.3.2.**
