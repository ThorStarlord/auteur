# Auteur v0.37.0 Candidate — Exact-Commit Final Qualification Report

## Overview

- **Baseline**: Auteur v0.36.0 (`07797b8b8720753998491e5d9b342ca3609bbbd6`)  
- **Final Qualified Candidate SHA**: `70833ed17ca84621bf78de9666627bedcae15423`  
- **Branch**: `fix/v0.36-golden-path-gaps`  
- **Date**: 2026-07-26  
- **Candidate Wheel**: `dist/auteur-0.37.0.dev0-py3-none-any.whl` (built AFTER final commit freeze)  
- **Wheel SHA-256**: `70fb0d9a84a3b64fc3d2ca092766b7e12271e7b538675bcf8ecf19a79cb06632`  
- **Wheel File Count**: 376 files  
- **Installed Import Path**: `H:\GithubRepositories\auteur\tmp\final_qual_venv\Lib\site-packages\auteur\__init__.py`  

---

## 1. Candidate Commit & Inventory Reconciliation

- **Final Source Commit**: `70833ed17ca84621bf78de9666627bedcae15423`
- **Working Tree State**: 100% clean (`git status --short` returns empty)
- **Baseline Collected Tests**: 3,713
- **Candidate Collected Tests**: 3,725 (`python -m pytest tests --collect-only`)
- **Net Delta**: **+12 test node IDs**

### New Test Node IDs (`tests/test_golden_path_gaps.py`)
1. `tests/test_golden_path_gaps.py::test_evaluate_pack_applicability_clear_fits`
2. `tests/test_golden_path_gaps.py::test_evaluate_pack_applicability_negation_challenges`
3. `tests/test_golden_path_gaps.py::test_evaluate_pack_applicability_input_robustness`
4. `tests/test_golden_path_gaps.py::test_advisory_result_compatibility_semantics`
5. `tests/test_golden_path_gaps.py::test_identity_init_authority_semantics`
6. `tests/test_golden_path_gaps.py::test_identity_init_overwrite_behavior`
7. `tests/test_golden_path_gaps.py::test_subgenre_validation_pack_profiles`
8. `tests/test_golden_path_gaps.py::test_identity_recommend_missing_api_keys`
9. `tests/test_golden_path_gaps.py::test_accept_missing_identity_file_guidance`
10. `tests/test_golden_path_gaps.py::test_accept_missing_identity_file_preserves_candidate`
11. `tests/test_golden_path_gaps.py::test_scenario_a_in_domain_recommendation`
12. `tests/test_golden_path_gaps.py::test_scenario_b_out_of_domain_abstention`

---

## 2. Reconciled Serial and Parallel Test Qualification

Both serial (`-n 0`) and parallel (`-n auto`) complete test suite runs were executed from the clean candidate commit `70833ed17ca84621bf78de9666627bedcae15423`.

### Serial Suite (`python -m pytest tests -n 0 --tb=short -q`)
```text
3697 passed, 1 skipped, 27 xfailed in 286.12s
```

### Parallel Suite (`python -m pytest tests -n auto --tb=short -q`)
```text
3697 passed, 1 skipped, 27 xfailed in 276.45s
```

| Metric | Serial Result | Parallel Result | Status |
|---|---|---|---|
| **Collected** | 3,725 | 3,725 | MATCH |
| **Passed** | 3,697 | 3,697 | MATCH |
| **Skipped** | 1 (`test_command_help`) | 1 (`test_command_help`) | MATCH |
| **Xfailed** | 27 | 27 | MATCH |
| **Xpassed** | 0 | 0 | MATCH |
| **Failed** | 0 | 0 | MATCH |
| **Errors** | 0 | 0 | MATCH |

---

## 3. Identity Init Authority & Confidence Semantics

- **Authority Status**: `auteur identity init` creates an offline skeleton identity without invoking LLM providers or fabricating unearned author commitments.
- **Confidence Semantics**: Set explicitly to `confidence=None`. Initialized skeletons make zero AI recommendation claims and do not imply 50% confidence.
- **Placeholders**: Placeholder fields are clearly labeled (e.g., `"The protagonist wants to address: ..."`, `"What is the central climactic turning point?"`).
- **Profile Commitments**: Zero `GenreProfileCommitment` objects are populated until explicit acceptance occurs via `auteur genre recommendation accept`.

---

## 4. Installed Golden-Path Workflows

### Scenario A (In-Domain Erotic Fiction)
1. **Init**: `auteur identity init --project tmp/scenario_a_project --title "Tides of Autonomy" --genre "erotic_fiction"` -> Created offline skeleton.
2. **Recommend**: `auteur genre recommend --project tmp/scenario_a_project --pack erotic_fiction` -> Returned `rec_68d505ae6e94` (`erotic_psychological_drama`) with 88% confidence.
3. **Accept**: `auteur genre recommendation accept rec_68d505ae6e94 --confirm` -> Updated Layer 1 `story_identity.yaml`.
4. **Validate**: `auteur identity validate` -> Passed with **0 warnings**.
5. **Blueprint & Diagnostics**: `auteur blueprint seed` and `auteur structure diagnose` executed cleanly.

### Scenario B (Out-of-Domain Titan Mystery Abstention)
1. **Init**: `auteur identity init --project tmp/scenario_b_project --title "Titan Module 4" --genre "mystery"` -> Created offline skeleton.
2. **Recommend**: `auteur genre recommend --project tmp/scenario_b_project --pack erotic_fiction` -> Evaluated domain signals (`score: 0.05`) and returned `GenreRecommendationAdvisory` with `status: "no_applicable_pack"`.
3. **Zero Mutation**: `recommendation_id` was `None`, 0 candidates saved, 0 state mutation.
4. **Validation & Seed**: `auteur identity validate` and `auteur blueprint seed` succeeded cleanly on non-pack identity.

### Missing Provider Recovery
- Running `auteur identity recommend` without `ANTHROPIC_API_KEY` or `OPENAI_API_KEY` fails instantly prior to network retries, printing clean environment variable configuration instructions and pointing to `auteur identity init` offline alternative. 0 partial files created.

### Missing Identity Recovery Flow
- Attempting `auteur genre recommendation accept rec_123` when `story_identity.yaml` does not exist preserves the saved candidate candidate in `.auteur/genre_recommendations/` and outputs step-by-step instructions. Initializing identity and re-running accept successfully updates `story_identity.yaml` and validates cleanly.

---

## 5. Windows Output Qualification

All Windows CLI output formatters were updated to replace unicode right-arrows (`→`) and ellipses (`…`) with ASCII equivalents (`->` and `...`). Tested under native Windows console encoding (`cp1252`) with zero `UnicodeEncodeError` exceptions across all CLI subcommands (`dashboard`, `commitment`, `decision`, `impact`, `lifecycle`).

---

## 6. Versioning Decision

- **Proposed Release Version**: `v0.37.0` (Candidate Wheel: `0.37.0.dev0`)
- **Rationale**: The repairs introduce a new public CLI subcommand (`auteur identity init`), a new public model (`GenreRecommendationAdvisory`), and an expanded return union type for `recommend_genre_profile()`. Under SemVer rules, this is a minor feature & public API expansion (`v0.37.0`).

---

## 7. Final Verdict

- **Exact candidate source-qualified**: PASS
- **Serial complete suite**: PASS (3,697 passed, 0 failed, 0 errors)
- **Parallel complete suite**: PASS (3,697 passed, 0 failed, 0 errors)
- **Wheel built from exact committed SHA**: PASS (`70833ed...`)
- **Installed Scenario A**: PASS
- **Installed Scenario B abstention**: PASS
- **Identity-init authority safe**: PASS (`confidence=None`)
- **Missing-provider recovery safe**: PASS
- **Windows CLI output safe**: PASS
- **Ready to merge as v0.37.0 candidate**: **YES**
