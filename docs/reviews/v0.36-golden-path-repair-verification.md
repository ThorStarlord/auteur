# Auteur v0.36 Golden-Path Repair Qualification & Verification

## Overview

- **Baseline**: Auteur v0.36.0 (`07797b8b8720753998491e5d9b342ca3609bbbd6`)  
- **Candidate SHA**: `87057aab57c326080a0ffe2160764a02da491ef5`  
- **Branch**: `fix/v0.36-golden-path-gaps`  
- **Date**: 2026-07-26  
- **Candidate Wheel**: `dist/auteur-0.37.0.dev0-py3-none-any.whl`  
- **Wheel SHA-256**: `037caff52a6d3ce03dbd8f05ec4a5818e555dcc041bdad1eb6cf3873a5289516`  
- **Wheel File Count**: 376 files  

All concrete golden-path gaps identified during the v0.36 dogfood review have been repaired, adversarially audited, qualified against the full test suite, built into an isolated candidate wheel, and verified in a fresh virtual environment.

---

## 1. Candidate Freeze & Inventory Reconciliation

- **Candidate Git SHA**: `87057aab57c326080a0ffe2160764a02da491ef5`
- **Baseline Collected Tests**: 3,713
- **Candidate Collected Tests**: 3,725 (`python -m pytest tests --collect-only`)
- **Net Delta**: +12 test node IDs

### New Test Node IDs (`tests/test_golden_path_gaps.py`)
1. `tests/test_golden_path_gaps.py::test_evaluate_pack_applicability_clear_fits`
2. `tests/test_golden_path_gaps.py::test_evaluate_pack_applicability_negation_challenges`
3. `tests/test_golden_path_gaps.py::test_evaluate_pack_applicability_input_robustness`
4. `tests/test_golden_path_gaps.py::test_advisory_result_compatibility_semantics`
5. `tests/test_golden_path_gaps.py::test_identity_init_authority_semantics`
6. `tests/test_golden_path_gaps.py::test_identity_init_overwrite_behavior`
7. `tests/test_golden_path_gaps.py::test_subgenre_validation_pack_profiles`
8. `tests/test_golden_path_gaps.py::test_identity_recommend_missing_api_keys`
9. `tests/test_golden_path_gaps.py::test_accept_missing_identity_file_guidance`
10. `tests/test_golden_path_gaps.py::test_accept_missing_identity_file_preserves_candidate`
11. `tests/test_golden_path_gaps.py::test_scenario_a_in_domain_recommendation`
12. `tests/test_golden_path_gaps.py::test_scenario_b_out_of_domain_abstention`

---

## 2. Reconciled Full-Suite Test Qualification

Ran full parallel pytest suite (`python -m pytest tests -n auto --tb=short -q`):

```text
================ 3697 passed, 1 skipped, 27 xfailed in 276.45s ================
```

- **Collected**: 3,725
- **Passed**: 3,697
- **Skipped**: 1 (`tests/test_cli_smoke.py::test_command_help`)
- **Xfailed**: 27 (expected structural/narrative analyzer xfails)
- **Xpassed**: 0
- **Failed**: 0
- **Errors**: 0

---

## 3. Detailed Audit Findings & Repairs

### Finding 1: `FORCED_GENRE_RECOMMENDATION`
- **Classification**: GOLDEN_PATH_GAP (P1)
- **Observed Cause**: `recommend_genre_profile()` lacked pack-level domain applicability evaluation prior to subgenre profile ranking.
- **Fix**: Implemented `evaluate_pack_applicability()` in `src/auteur/genre_packs/recommendation.py`. When domain relevance score is below threshold (`< 0.30`), the engine abstains and returns a `GenreRecommendationAdvisory` (`status="no_applicable_pack"`).
- **Adversarial Audit**: Verified clear positive fits, out-of-domain abstentions, negations ("rejects desire", "avoids intimacy"), and boundary strings (10,000-char text, Unicode emojis).
- **Evidence**: Scenario B (Titan mystery premise) returns `GenreRecommendationAdvisory` with `status: "no_applicable_pack"`, score 0.05, 0 state mutation.

### Finding 2: `IDENTITY_RECOMMEND_REQUIRES_LLM_KEY`
- **Classification**: GOLDEN_PATH_GAP / USABILITY_FRICTION (P1)
- **Observed Cause**: `auteur identity recommend` initiated LLM calls without checking provider API keys, causing hidden retries and 4-attempt failures.
- **Fix**: Added upfront environment credential check in `src/auteur/cli_dispatch.py` for `identity recommend`. If `ANTHROPIC_API_KEY` or `OPENAI_API_KEY` is missing, it outputs concise setup instructions and points to `auteur identity init`. Implemented `handle_identity_init()` and registered `auteur identity init` CLI subcommand for offline skeleton generation.
- **Authority Audit**: `identity init` sets `confidence=0.5`, labels placeholder fields explicitly, preserves exact user title and genre, and creates ZERO unearned `GenreProfileCommitment` objects.
- **Evidence**: Missing keys trigger instant, friendly setup guidance. `identity init` creates editable `story_identity.yaml` offline.

### Finding 3: `RECOMMENDATION_ACCEPT_REQUIRES_EXISTING_IDENTITY_FILE`
- **Classification**: GOLDEN_PATH_GAP / USABILITY_FRICTION (P2)
- **Observed Cause**: `auteur genre recommendation accept` raised `[PACK_NOT_FOUND]` error when `story_identity.yaml` did not exist.
- **Fix**: Updated `dispatch_genre_pack_commands()` in `src/auteur/genre_packs/cli.py`. If `story_identity.yaml` is missing, it preserves the saved recommendation candidate in `.auteur/genre_recommendations/` and outputs clear, actionable guidance explaining how to run `auteur identity init` first.
- **Evidence**: Candidate file remains untouched on disk; CLI returns exact recovery steps.

### Finding 4: `SUBGENRE_VALIDATION_UNKNOWN_WARNING_FOR_PACK_PROFILES`
- **Classification**: DEFECT (P2)
- **Observed Cause**: `StoryIdentity.validate_identity()` checked legacy subgenre modifiers but neglected registered Genre Pack profile IDs.
- **Fix**: Updated subgenre validation in `src/auteur/identity.py` to inspect registered Genre Pack profile IDs via `get_pack_registry()`.
- **Evidence**: Validating a `StoryIdentity` containing `subgenres: [erotic_psychological_drama]` passes with 0 warnings.

---

## 4. Versioning Decision

- **Recommended Release Version**: `v0.37.0` (Candidate Wheel: `0.37.0.dev0`)
- **Rationale**:
  1. `auteur identity init` introduces a new public CLI subcommand.
  2. `GenreRecommendationAdvisory` adds a new public schema model and union return type for `recommend_genre_profile()`.
  3. Public return contract and CLI output schema were expanded to support advisory payloads.
  Under Semantic Versioning rules, this constitutes a minor feature and public API extension (`v0.37.0`).

---

## 5. Candidate Wheel & Fresh Qualification Result

- **Build Tool**: `hatchling` via `python -m build --wheel`
- **Wheel Artifact**: `dist/auteur-0.37.0.dev0-py3-none-any.whl`
- **SHA-256**: `037caff52a6d3ce03dbd8f05ec4a5818e555dcc041bdad1eb6cf3873a5289516`
- **File Count**: 376 files
- **Fresh Qualification**: Created isolated virtual environment `tmp/fresh_venv`, installed candidate wheel, and verified offline skeleton initialization outside repo root. Result: **PASSED**.

---

## 6. Final Qualification Verdict

- **Forced irrelevant recommendations fixed**: PASS
- **Honest abstention supported**: PASS
- **Opinionated in-domain behavior preserved**: PASS
- **Built-in profile validation fixed**: PASS
- **Missing-provider guidance usable**: PASS
- **No incomplete Identity fabrication**: PASS
- **Wheel isolated execution verified**: PASS
- **Full test suite qualified**: PASS (3,697 passed, 0 failed, 0 errors)
- **Final Status**: **QUALIFIED FOR RELEASE MERGE AS v0.37.0**
