# Pre-source snapshot correction record

The original `session-b-pre-source-reconstruction-snapshot.md` was created
after probing the worktree root as the project path. That probe was invalid for
persisted Auteur state; it is retained unchanged as experimental evidence.

The actual persisted project endpoint is:

`experiments/dense-trilogy-architecture-stress-test-v1`

Before any raw narrative artifact was opened, the corrected normal surfaces
were run there. Exact output is frozen in
`session-b-corrected-initial-surface-output.txt`.

The corrected pre-source understanding is:

- Accepted-history projection recovers twelve Book-1 facts, including Mara's
  changed authority, the erased coast's contested status, divided Admiralty
  legitimacy, Mara/Ion conditional alliance, Nera's active protection, the
  Brann and Lio anomaly facts, and institutional/location pressures.
- Map, Focus, and continuity review are intentionally unavailable until an
  explicit Book-2 planning intent exists.
- No character/faction/location/thread detail beyond those twelve surfaced
  facts is assumed before source lookup.

This correction is a path-fix record, not a revision of the original snapshot.
